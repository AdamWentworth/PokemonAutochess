# leech_seed_config.cfg
# Percent of target max HP to sap per tick (0.05 = 5%)

# Total duration of the seed effect
# - If 0 or negative, no duration cap (not recommended)

# Minimum sap amount per tick (integer)

# Healing multiplier (1.0 = heal equal to sap; 0.5 = heal half)

sapPercent=0.08
tickIntervalSec=2.0
durationSec=6.0
minSap=1
healMultiplier=1.0
