# Runtime Mesh Assets

Place runtime-resolved effect meshes here when code or config references them
directly.

Examples:
- `assets/meshes/growl_*.glb`

If a mesh is still source/reference content, keep it under `assets/vfx/` until
the runtime path is finalized.
