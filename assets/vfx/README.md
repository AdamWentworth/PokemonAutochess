# Reusable VFX Assets

Use this folder for reusable/reference/staging VFX assets and subfolder
organization.

Rules:
- Promote concrete runtime meshes into `assets/meshes/`.
- Promote concrete runtime textures into `assets/textures/`.
- Keep source-like or reusable library assets here until a specific runtime path
  is chosen.
