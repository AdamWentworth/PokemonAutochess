# Runtime Texture Assets

Place runtime-resolved textures here when code or config references them
directly.

Examples:
- Charmander-line authored fire flipbooks
- `assets/textures/moves/growl/*`

If a texture is still exploratory or meant to stay as reusable reference art,
keep it under `assets/vfx/` until it becomes part of a concrete runtime path.
